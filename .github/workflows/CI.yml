name: CI

on: 
  push:
    branches:
      - main

jobs:
  test-automatico:
    name: 'Jest test'
    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm run test
